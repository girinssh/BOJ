#include <iostream>
#include <list>

using namespace std;

int main(void) {
	cin.tie(NULL);
	cin.sync_with_stdio(false);


	while (true) {
		int n, k, m;

		cin >> n >> k >> m;

		list<int> l;
		list<int>::iterator iter;



	}
}