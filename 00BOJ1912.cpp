#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main(void) {
	int n;
	int maxVal = 0;

	cin.tie(NULL);
	cin.sync_with_stdio(false);

	cin >> n;

	vector<int> v(n);
	vector<int> dp(n);

	for (int i = 0; i < n; i++) {
		cin >> v[i];

		
	}

	cout << maxVal;

	return 0;

}
